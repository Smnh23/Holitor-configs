spring:
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
  jpa:
    show-sql: true
    open-in-view: true
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
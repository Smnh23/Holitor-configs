server:
  port: 9102

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVER:http://localhost:9101/eureka/}
      
spring:
  cloud:
    gateway:
      routes:
      - id: ms-product
        uri: lb://MS-PRODUCT
        predicates:
        - Path=/api/product/**